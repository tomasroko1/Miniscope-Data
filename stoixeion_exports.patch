diff --git a/Stoixeion.m b/Stoixeion.m
--- a/Stoixeion.m
+++ b/Stoixeion.m
@@ -1,2 +1,2 @@
-function [Pools_coords] = Stoixeion(Spikes,Coord_active,FFo)
+function [Pools_coords,Diagnostics] = Stoixeion(Spikes,Coord_active,FFo)
 % Find ensembles using SVD method.
@@ -93,3 +93,3 @@
 % Find the peaks in the states and the cells in the states
-[C_edos,sec_Pk_edos] = Edos_from_Sindex_svd(S_indexp,state_cut); %,edos_size_cut,edos_svd_cut,rep_svd);
+[C_edos,sec_Pk_edos,singular_values,selected_ranks] = Edos_from_Sindex_svd(S_indexp,state_cut); %,edos_size_cut,edos_svd_cut,rep_svd);
 % the returned C_edos is a binary num_sig_frame-by-num_state matrix, where
@@ -178,3 +178,15 @@
 [Cells_coords,Pools_coords] = Search_edos_coords(Cells_edos,sis_query,Coord_active);
-
+
+Diagnostics = struct();
+Diagnostics.pks = pks;
+Diagnostics.significant_frames = Pks_Frame;
+Diagnostics.scut = scut;
+Diagnostics.hcut = hcut;
+Diagnostics.singular_values = singular_values;
+Diagnostics.selected_singular_ranks = selected_ranks;
+Diagnostics.ensemble_vectors = C_edos;
+Diagnostics.ensemble_labels = sec_Pk_frames;
+Diagnostics.all_ensemble_cells = Cells_edos;
+Diagnostics.core_search = sis_query;
+
 % plot cross-correlations within and between ensembles
diff --git a/AGORA/Edos_from_Sindex_svd.m b/AGORA/Edos_from_Sindex_svd.m
--- a/AGORA/Edos_from_Sindex_svd.m
+++ b/AGORA/Edos_from_Sindex_svd.m
@@ -1 +1 @@
-function [C_edos,sec_Pk_edos] = Edos_from_Sindex_svd(S_indexp,state_cut)
+function [C_edos,sec_Pk_edos,singular_values,selected_ranks] = Edos_from_Sindex_svd(S_indexp,state_cut)
@@ -25,3 +25,4 @@
 % do SVD on binarized similarity matrix
 sz = size(S_indexp,2);
 [U_svd,S_svd,V_svd] = svd(S_indexp);
+singular_values = diag(S_svd);
@@ -85,3 +85,4 @@
 end
-
+
+selected_ranks = edos_temp2(:);
 %fac_cut=0.85;
